# API Base URL (local dev: http://localhost:3000, production: https://api.firatech.systems)
# Baked in at build time by Vite. Set it in the Vercel project's environment before build.
VITE_API_BASE_URL=http://localhost:3000